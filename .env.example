# Optional defaults for the CLI. OCI authentication still comes from ~/.oci/config.
OCI_CONFIG_FILE=~/.oci/config
OCI_CONFIG_PROFILE=DEFAULT
ODA_INSTANCE_ID=ocid1.odainstance.oc1..<replace-me>
# Required only for the signed ODA REST command.
ODA_REST_ENDPOINT=https://<your-oda-service-host>
